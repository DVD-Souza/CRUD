<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Excluir Registro</title>
    <link rel="stylesheet" href="css/styles.css">
    <style>
        /* Global settings */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f9f9f9;
            color: #333;
            line-height: 1.6;
            padding: 20px;
        }

        /* Main container */
        .container {
            max-width: 900px;
            margin: 0 auto;
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Heading */
        h1 {
            text-align: center;
            color: #FF0000;
            margin-bottom: 20px;
        }

        /* Instructions text */
        p {
            font-size: 18px;
            text-align: center;
            margin-bottom: 20px;
        }

        /* Details section */
        .details {
            font-size: 16px;
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
        }

        /* Delete button */
        button {
            padding: 10px 20px;
            font-size: 16px;
            background-color: #FF0000;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            width: 100%;
            max-width: 300px;
            display: block;
            margin: 0 auto;
        }

        button:hover {
            background-color: #d40000;
        }

        /* Responsive adjustments */
        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            button {
                max-width: 100%;
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Excluir Registro</h1>
        <p>Tem certeza que deseja excluir este registro?</p>
        <p class="details"><%= registration.nome %> : <%= registration.funcao %></p>
        <form action="/delete/<%= registration._id %>?_method=DELETE" method="post">
            <button type="submit">Excluir</button>
        </form>
    </div>
</body>
</html>
